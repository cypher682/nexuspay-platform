feat: payments<->notifications event link, email verification, password reset

Payments -> RabbitMQ -> notifications receipt/failure emails
- payments-service: add amqplib domain-event publisher (topic exchange
  nexuspay.events, payment.succeeded / payment.failed), publish on terminal
  worker transition; store recipientEmail on the payment (from the access-token
  email claim now added to auth access JWTs + forwarded through the gateway);
  add RABBITMQ_URL/EVENTS_EXCHANGE env + one prisma migration
- notifications-service: rewrite rabbitmq lib with DLX (nexuspay.dlx) so failed
  deliveries route to a dead-letter queue instead of being dropped; add a
  domain-event topic consumer (payment.#, auth.#) that enqueues
  payment_receipt/failed and verify/password-reset emails; new templates

Auth-service completeness
- replace /v1/auth/verify-email 501 stub with a real Redis/DB token flow
  (register generates a token, auth.user_registered event carries the verify
  link, VERIFICATION_TOKEN_TTL_HOURS window, flips isVerified + clears token)
- add /v1/auth/forgot-password and /v1/auth/reset-password; reset tokens are
  stored hashed in Redis with TTL, revoke all refresh families on reset

Gateway
- expose forgot-password / reset-password as public auth routes; createAccessToken
  now carries the email claim (payments reads it for receipt delivery)

Compose/env: wire RABBITMQ_URL into auth + payments (depends_on rabbitmq),
document RABBITMQ_URL/PUBLIC_BASE_URL; lockfiles/npm ci re-verified on linux

All 4 services: eslint 0, tsc 0, jest green (5/11/7/4); npm audit 0; migrations
applied locally.