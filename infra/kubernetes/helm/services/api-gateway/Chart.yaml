apiVersion: v2
name: api-gateway
description: NexusPay edge gateway (JWT validation, rate limiting, routing, aggregation)
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: nexuspay-lib
    version: 0.1.0
    repository: file://../../nexuspay-lib
